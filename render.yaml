services:
  - type: worker
    name: habit-tracker-bot
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: TZ
        value: Europe/Moscow
    disk:
      name: habit-data
      mountPath: /data
      sizeGB: 1
